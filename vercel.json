{
  "builds": [
    {
      "src": "angular.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist/helpdesk-v2" }
    }
  ],
  "installCommand": "npm install --ignore-scripts",
  "rewrites": [{ "source": "/(.*)", "destination": "/" }]
}
